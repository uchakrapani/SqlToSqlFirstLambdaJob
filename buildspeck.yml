version: 0.2

phases:
  install:
    runtime-versions:
      dotnet: 3.1
  build:
    commands:
      - dotnet restore
      - dotnet build --configuration Release
      - dotnet lambda package --configuration Release --output-package output.zip

artifacts:
  files:
    - output.zip